name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  lint-typecheck-test:
    name: Lint, typecheck, and test
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v3
        with:
          version: "latest"

      - name: Set up Python 3.11
        run: uv python install 3.11

      - name: Install dependencies
        run: uv sync --all-extras

      - name: Lint with ruff
        run: uv run ruff check src/ projects/ tests/

      - name: Check formatting
        run: uv run ruff format --check src/ projects/ tests/

      - name: Type check with mypy
        run: uv run mypy src/ --ignore-missing-imports

      - name: Run tests
        run: |
          PYTHONPATH=projects/01_demand_forecasting/src:projects/02_returns_intelligence/src:projects/03_checkout_intent/src \
          uv run pytest tests/ projects/ -q --tb=short --ignore=projects/01_demand_forecasting/notebooks --ignore=projects/02_returns_intelligence/notebooks --ignore=projects/03_checkout_intent/notebooks
