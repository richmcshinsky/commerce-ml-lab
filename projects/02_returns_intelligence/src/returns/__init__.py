"""Returns Intelligence module for Project 02.

Three models sharing one synthetic data schema:

likelihood
    P(return | order features) — return likelihood scoring.
fraud
    Returns abuse detection with graph features and cost-aware thresholding.
exchange
    Reason-conditioned exchange recommendation engine.
"""
